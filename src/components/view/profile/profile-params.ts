const profileParams = {
  section: {
    tag: 'section',
    cssClasses: ['profile'],
  },

  wrapper: {
    tag: 'div',
    cssClasses: ['profile__wrapper', 'wrapper'],
    textContent: 'PROFILE IS HERE',
  },
};

export default profileParams;
