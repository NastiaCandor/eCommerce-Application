/* const { describe, expect, test } = require('@types/jest');

const sum = require('./test-func');

describe('sum module', () => {
  test('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toBe(3);
  });
});

husky pre-commit auto-tests disabled for now. needs a proper configuration and dependecies for a jest
*/
